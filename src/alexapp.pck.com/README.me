When pulling the project from scratch first:

1. Create folder with app name (ex: GoApi)
2. Create src folder in it (ex: GoApi/src)
3. Create package name in src folder (ex: GoApi/src/alexapp.pck.com)
4. cd into GoApi/src/alexapp.pck.com and run "git pull" * this is where the repo should reside
5. cd into GoApi/src/alexapp.pck.com/app and run "go install" --this will create all the packages needed (pkg, bin folders)
6. Run ./bin/app executable

